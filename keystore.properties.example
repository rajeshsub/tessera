# Copy to keystore.properties and fill in for release signing.
# keystore.properties is git-ignored and must never be committed.
# For debug builds, bootstrap generates a debug keystore automatically; this file is not required.

storeFile=release.jks
storePassword=CHANGE_ME
keyAlias=tessera
keyPassword=CHANGE_ME
